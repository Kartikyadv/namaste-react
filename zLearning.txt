useEffect
useState

react router dom
1. import { RouterProvider, createBrowserRouter } from "react-router-dom";

2. const appRouter = createBrowserRouter([
    {
        path: "/",
        element: <App/>
    },
    {
        path: "/about",
        element: <About/>
    },
    {
        path: "/contact",
        element: <Contact/>
    },
])

3. root.render(<RouterProvider router={appRouter}/>);

useRouteError
Outlet & children
Link

2 types of ROUTING
1. CLIENT side routing - all pages in one go (Reactjs)
2. SERVER side routing - every page makes an API call (nextJS)

useParams hook