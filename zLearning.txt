1. State managing library , not in react, not necc-essary 
    useContext can be used alternatively

Redux Toolkit
- Install  @redux/toolkit and react-redux
- Build our store
-  Connect store and App
- Slice(cartSlice)
- dispatch(Action)
- Selector

useDispatch- dispatch a action to run a reducer f(n)


onclick={handleItem} - will run when btn is clicked
onclick={handleItem(item)} - will run when page is rendered w/o clicking button
onclick={() => handleItem(item)} - this will run when page is rendered but arrow
               f(n) creates a closure and capture the value returned/f(n) runned